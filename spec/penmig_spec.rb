require 'spec_helper'

describe PendMigration do
  it 'has a version number' do
    expect(PendMigration::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
